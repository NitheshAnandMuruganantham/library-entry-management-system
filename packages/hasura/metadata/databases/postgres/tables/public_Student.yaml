table:
  schema: public
  name: Student
array_relationships:
  - name: attendances
    using:
      foreign_key_constraint_on:
        column: student_id
        table:
          schema: public
          name: attendance
